name: Manual Multi Jobs Demo

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build step
        run: echo "✅ Build job running"

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Test step
        run: echo "🧪 Test job running"

  lint:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Lint step
        run: echo "🧹 Lint job running"

  deploy:
    runs-on: ubuntu-latest
    needs: [test, lint]
    steps:
      - name: Deploy step
        run: echo "🚀 Deploy job running"
