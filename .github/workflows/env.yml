name: environment

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  env:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Print environment info
        run: |
          echo "USER=$USER"
          echo "RUNNER_OS=$RUNNER_OS"
